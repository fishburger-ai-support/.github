# ENIGMA HACK — AI-агент для автоматизации обработки писем техподдержки

## 📌 О проекте

AI-агент для автоматической обработки входящих писем технической поддержки.

Система:
- анализирует письмо,
- извлекает ключевые данные,
- определяет тональность,
- создает тикет в базе,
- ищет решение в базе знаний (RAG),
- генерирует ответ,
- отправляет его автоматически или передает оператору.

Цель — сократить время обработки обращений на 50% и повысить качество ответов благодаря анализу базы знаний.

---

## 👥 Команда

- **Александр** — Backend  
- **Арина** — Frontend  
- **Вероника** — Frontend  
- **Аня** — ML  

---

## 🧭 User Flow

1. Клиент отправляет письмо на почту поддержки.
2. Backend получает письмо (IMAP).
3. Письмо отправляется в GigaChat API.
4. AI:
   - извлекает ФИО, контакты, объект, номера приборов,
   - определяет тональность,
   - формирует JSON.
5. Backend создаёт тикет в БД (статус: `Новый`).
6. AI выполняет RAG-поиск по базе знаний.
7. Генерируется ответ.

### Возможные сценарии:

- ✅ Данных достаточно → письмо отправляется клиенту → статус `Закрыт`
- ⚠ Недостаточно данных → клиенту отправляется запрос на уточнение
- 🚨 Низкая уверенность → тикет передается оператору

---

## 🏗 Архитектура

**Компоненты:**

- Email Connector (IMAP / SMTP)
- Backend (Flask)
- GigaChat API (анализ + генерация)
- RAG-модуль (Sentence Transformers, in-memory)
- PostgreSQL
- Frontend (React)

### Логика:

Email → Backend → AI-анализ → БД → RAG → Генерация ответа →  
Автоответ / Уточнение / Передача оператору

---

## 📊 Структура тикета

- Дата
- ФИО
- Объект
- Телефон
- Email
- Заводские номера
- Тип прибора
- Тональность
- Суть вопроса
- Статус
- Черновик ответа

---

## ⚠ Риски и минимизация

### 1. Неизвестный формат существующей базы данных

База данных предоставляется организаторами, и её структура заранее неизвестна.  
Возможны несоответствия в названиях полей, типах данных и связях между таблицами.

**Минимизация:**
- проектируем слой адаптации (Data Mapper / Repository Layer);
- изолируем бизнес-логику от структуры БД;
- закладываем время на быструю миграцию или маппинг полей.

---

### 2. Интеграция с внешним AI-сервисом (GigaChat)

Скорость ответа, стабильность и лимиты запросов зависят от внешнего API, на которое мы не можем повлиять.

Риски:
- задержки ответа,
- нестабильность сервиса,
- ограничения по количеству запросов.

**Минимизация:**
- асинхронные запросы;
- таймауты и обработка ошибок;
- логирование всех вызовов;
- fallback-сценарий (передача оператору при сбое AI).

---

### 3. Непредсказуемый формат входящих писем

Письма могут содержать:
- несколько приборов,
- несколько контактных лиц,
- неполные данные,
- вложения вместо текста.

**Минимизация:**
- строгий формат JSON-ответа от AI;
- валидация данных перед записью в БД;
- возможность ручной корректировки оператором.

---

### 4. Ограниченное время хакатона

Риск не успеть реализовать весь функционал.

**Минимизация:**
- приоритет MVP:
  1. получение письма,
  2. создание тикета,
  3. генерация ответа,
  4. простой интерфейс оператора;
- модульная архитектура для быстрого упрощения логики.


---

## 🛠 Технологии

- Python / Flask
- GigaChat API
- Sentence Transformers
- PostgreSQL
- React
- Docker

---

## 🎯 Ожидаемый результат

- Сокращение времени обработки писем до 50%
- Повышение качества ответов
- Структурированная база обращений
- Масштабируемая архитектура
